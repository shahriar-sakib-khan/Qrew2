<USER_REQUEST>
You are a senior full-stack TypeScript engineer embedded in an existing
production monorepo. You are building the Formula-Driven Invoice Engine —
a completely new feature. Nothing for this feature exists yet except for
some primitive scaffolding in the invoices schema that you will replace.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WORKFLOW — READ THIS FIRST
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

We work in micro steps. Each step has a single, clearly scoped output.

1. I give you the step number and description.
2. You audit the relevant files in the codebase before writing anything.
3. You implement exactly that step — nothing more.
4. You show me all changed/created files.
5. You state the verification test (what I should run or check in the browser).
6. You wait. You do not proceed to the next step.
7. I confirm, then give you the next step.

If you discover something in the codebase that contradicts the architecture
described below, flag it before writing code. Do not work around it silently.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CODEBASE AUDIT — READ THESE FILES NOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Before we start, read the following files and confirm you have understood them:

packages/db/src/
  index.ts                        ← how the DB client is created and schemas exported
  schema/auth.ts                  ← users, organizations, members tables
  schema/projects.ts              ← the "File" entity (projects = business files)
  schema/expenses.ts              ← expenses against projects
  schema/expense_categories.ts    ← expense category registry per org
  schema/invoices.ts              ← PRIMITIVE SCHEMA — you will replace this entirely
  schema/clients.ts 
<truncated 38100 bytes>
toricalFormat
            - frozenAt, schemaVersion display

  [ ] 11.3 Backend: PDF Generation
            - @react-pdf/renderer on the server side
            - Reads from: invoice_line_items + resolvedHeaderValues + invoice_pdf_layouts
            - Renders: company header (logo, name), recipient block, header fields block,
              line items table (two columns), bank details, extra sections, footer blocks
            GET /api/invoices/:id/pdf → returns PDF buffer (application/pdf)

  [ ] 11.4 Backend: PDF Layout Preview
            GET /api/invoice-pdf-layouts/preview
            → generates sample PDF with dummy data using current layout config
            FINAL CHECKPOINT: End-to-end flow complete. All 5 goals verified.

====================================================================

TOTAL: ~40 micro steps across 12 phases
Each step: one agent session, one clear output, one verification point.
====================================================================

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
YOUR FIRST TASK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Start with Step 0.1.

First, audit the files I listed at the top and confirm your findings.
Then implement Step 0.1: create all six Postgres enums in a new file at
packages/db/src/schema/invoice-enums.ts and export them from the schema
barrel. Show me the file and confirm TypeScript compiles cleanly.

Do not write any table definitions yet.
Wait for my confirmation before proceeding to Step 0.2.
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-06-10T11:42:25+06:00.
</ADDITIONAL_METADATA>
<USER_SETTINGS_CHANGE>
The user changed setting `Model Selection` from None to Gemini 3.1 Pro (High). No need to comment on this change if the user doesn't ask about it. If reporting what model you are, please use a human readable name instead of the exact string.
</USER_SETTINGS_CHANGE>