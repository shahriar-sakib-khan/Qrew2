/**
 * dashboard/inventory/layout.tsx
 * Minimal layout — no sub-navigation bar.
 * Navigation is now handled by the collapsible SidebarNavGroup in the sidebar.
 */

export default function InventoryLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
